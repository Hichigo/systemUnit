window.onload = ->
	# expandBlock = document.getElementsByClassName('expand-js')
	# for i in [0...expandBlock.length]
	# 	expandBlock[i].onclick = ->
	# 		# if not@.parentNode.classList.contains('auto-height')
	# 		# 	@.parentNode.classList.add('auto-height')
	# 		# else
	# 		# 	@.parentNode.classList.remove('auto-height')
	# 		@.parentNode.classList.toggle 'auto-height'
	# 		return
	return